# dotnet
- LocalRecitationCore must target .NET 9.0 (matching the global.json SDK version 9.0.315), not .NET 10.0. Explicitly motivated by compatibility — the user values SDK-level compatibility over bleeding-edge tooling. Confidence: 0.80
- When retargeting one project's TFM for SDK compatibility, check and fix all projects in the solution — don't leave any project targeting an unsupported SDK. Confidence: 0.65
- Windows build and test verification consistently uses the exact command pattern `dotnet build ... --framework net9.0-windows10.0.19041.0 --runtime win-x64 --no-restore` and `dotnet test ... --framework net9.0-windows10.0.19041.0 --runtime win-x64 --no-restore` — not just a bare `dotnet build`. Confidence: 0.80
- When extending a large existing service class (e.g., VerseRepository), prefer adding new domain operations in a separate partial-class file (e.g., `VerseRepository.LearningDomain.cs`) rather than bloating the original file. Confidence: 0.65
- For SQLite schema evolution, use additive `CREATE TABLE IF NOT EXISTS` definitions with foreign keys and indexes — never destructive migrations. Confidence: 0.70
- For integration-style repository tests, use isolated SQLite database files with unique names (e.g., `Guid.NewGuid():N` suffix) and call `SqliteConnection.ClearAllPools()` before deleting the test database to avoid handle-leak failures. Confidence: 0.75
- Arabic/Quranic text literals in C# source files should use Unicode escape sequences (e.g., `\u0642`) rather than raw Arabic characters, to prevent encoding corruption across editors, terminals, and build pipelines. Confidence: 0.75
- When reading parent/child rows from SQLite, materialize parent rows into an intermediate list first (closing the reader), then query child rows separately — never nest child queries inside an open parent reader, which causes "already an open DataReader" errors. Confidence: 0.70
- When MVVM Toolkit `[ObservableProperty]` on fields generates MVVMTK0045 AOT warnings, convert to explicit `ObservableObject` properties with `SetProperty` rather than using the newer partial-property syntax — the project's toolkit version doesn't support partial properties. Preserve `OnXChanged` callbacks as private methods invoked from setters, and manually raise `OnPropertyChanged` for any derived properties that depend on the changed value. Confidence: 0.80
- For MAUI Pickers bound to async-loaded data, use `ObservableCollection<T>` as the ItemsSource and populate it on the UI thread via `MainThread.InvokeOnMainThreadAsync` — mutating a plain `List<T>` in-place and raising `OnPropertyChanged` is unreliable because native controls may retain their initial empty state. Confidence: 0.75
- The repository uses CRLF line endings; after file edits that may leave LF-only line endings, normalize back to CRLF (e.g., via PowerShell `$text -replace \"`r?`n\", \"`r`n\"`) to keep `git diff --check` clean. Confidence: 0.70
- Strive for zero-warning builds — when XAML compiled bindings produce warnings (e.g., `Source={x:Reference}` inside a CollectionView ItemTemplate), replace the warning-producing pattern with a code-behind `Clicked` handler that retrieves the BindingContext, rather than suppressing or ignoring the warning. Confidence: 0.70
- When NuGet XML documentation is insufficient to determine a library's API surface, use PowerShell reflection (`Add-Type -Path`, `.GetProperties()`, `.GetFields()`, `.GetElementType()` for arrays) against the DLL to discover type members — prefer this over guessing or deferring to build-time. Confidence: 0.65
- For final verification, chain build and full test suite in a single shell command with `&&` (e.g., `dotnet build ... && dotnet test ...`) rather than issuing two separate commands. Confidence: 0.55
- When making performance-sensitive decisions (e.g., enabling optional CPU-intensive features like DTW token timestamps), measure actual runtime performance via real-time factors or wall-clock timing rather than inferring capability from static hardware specs (CPU cores, RAM size). Confidence: 0.65
- When adding a feature that could degrade core pipeline performance (e.g., inference latency), gate it behind a config toggle, default it to off, and enable it dynamically only when runtime measurements prove sufficient headroom — the core experience must never be sacrificed for optional precision. Confidence: 0.65
- Tests access internal members via `[assembly: InternalsVisibleTo("TestProject")]` in a dedicated `Properties/AssemblyInfo.cs` file, and the production class is made `partial` to separate test hooks from normal code — avoid csproj `<InternalsVisibleTo>` items and never make test-only members public. Confidence: 0.70
- For additive SQLite schema changes on existing tables, guard `ALTER TABLE ADD COLUMN` with a `PRAGMA table_info(...)` check for column existence first — SQLite's `ALTER TABLE` has no `IF NOT EXISTS`, and blindly re-running the ALTER causes "duplicate column name" errors on fresh databases that already include the column in `CREATE TABLE`. Confidence: 0.70
